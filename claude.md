# Social Bounty MVP

## Project Overview

Social Bounty is a bounty-based marketplace platform where businesses create tasks with rewards, users complete them and submit proof, businesses review submissions, and super admins manage the platform. This is a production-bound MVP.

**Specification**: See `md-files/social-bounty-mvp.md` for the full product spec.

## Tech Stack

- **Front-end**: Next.js 14 (App Router), PrimeReact 10, Tailwind CSS, TypeScript, TanStack Query 5
- **Back-end**: Node.js (NestJS), PostgreSQL, Prisma ORM, REST API, Redis (TradeSafe OAuth token cache + webhook replay guard)
- **Payments**: TradeSafe (inbound + outbound unified rail per ADR 0011). `TRADESAFE_MOCK=true` on Render until prod OAuth creds arrive (R24).
- **Webhooks**: TradeSafe URL-path-secret (`crypto.timingSafeEqual`) + authoritative GraphQL re-fetch for state; idempotency via `WebhookEvent.UNIQUE(provider, externalEventId)`.
- **Infrastructure**: Local / Staging / Production, Sentry for error tracking
- **Auth**: JWT (access + refresh), email verification via OTP. Password-auth implementation is roadmap — see `docs/reviews/2026-04-15-team-lead-audit-batch-13.md` for the current gap.

## Hard Rules

1. **MVP only** - no feature creep. If it's not in the spec, don't build it.
2. **RBAC mandatory** on every screen and API endpoint.
3. **Audit logs required** for all admin actions and status changes.
4. **100% test pass rate** required before any release.
5. **PrimeReact + Tailwind CSS** must be used for all UI.
6. All destructive actions require confirmation dialogs.
7. Document assumptions; don't invent requirements.
8. **Use agents for every task and run them in paralell** md-files/agents/agent-overview.md

## User Roles

- **Participant**: Browse bounties, submit proof, track submissions
- **Business Admin**: Create/manage bounties, review submissions, manage payouts
- **Super Admin**: Full platform access, user/org management, audit logs, troubleshooting

## Data Model Entities

User, Brand, BrandMember, Bounty, Submission, AuditLog, FileUpload, LedgerEntry, LedgerTransactionGroup, WebhookEvent, TradeSafeTransaction, TradeSafeAllocation, Refund, RecurringIssue, JobRun, SystemSetting. (Subscription / SubscriptionPayment entities retained in schema for future Pro-tier work; the Pro upgrade flow is gated behind a "coming soon" placeholder per ADR 0011.)

> Organisation was renamed to Brand in commit `539467e` (2026-04-15). A legacy JWT claim compatibility shim lives in commit `8c50d38` for existing sessions. The residual `/business/organisation/*` routes were consolidated into `/business/brands/*` in commit `8e4c21f` (2026-04-16).

## Project Structure

```
social-bounty/
  claude.md         # This file — project charter + financial non-negotiables
  apps/
    web/            # Next.js front-end
    api/            # NestJS back-end
  packages/
    shared/         # Shared types, DTOs, enums, ledger constants
    prisma/         # Prisma schema and migrations
  docs/             # Architecture docs, ADRs, reviews, runbooks
    adr/            # Architectural Decision Records (0001–0011)
    reviews/        # Team-lead audit reports (2026-04-15 batches 2–13)
    perf/           # Reconciliation benchmarks, perf evidence
  md-files/         # Product + domain specs and agent play-books
    agents/         # 9 per-role agent specs (entry: agent-overview.md)
    archive/        # Superseded historical docs (dated filenames)
    *.md            # Canonical specs: financial-architecture,
                    # knowledge-base, admin-dashboard, implementation-phases,
                    # social-bounty-mvp, brand-profile-and-signup,
                    # AGENTS (team roster), SPRINT-PLAN, RELEASE-NOTES
                    # (payment-gateway.md removed 2026-04-24 — ADR 0011 single-rail
                    # TradeSafe cutover; canonical payment spec is now ADR 0011 itself)
                    # — design system lives at apps/web/src/styles/design-system/
  scripts/          # CLI tools (kb-context.ts, bench-reconciliation.ts)
```

## Design System Viewer

A standalone Vite + React app at `apps/design-system/` for browsing all design system tokens, components, and patterns interactively. Dev-only — not deployed.

```bash
npm run dev:ds          # starts at http://localhost:5173
```

Sections: Colors, Typography, Spacing & Radii, Components (live instances with toggleable props), Patterns (tables, feeds, receipts, forms), Icons (Lucide reference grid). Includes a dark mode toggle. CSS is imported directly from `apps/web/src/styles/design-system/` — changes are reflected on refresh.

## Conventions

- TypeScript strict mode everywhere
- Use Prisma for all database access
- REST API with consistent error response format
- Input validation at API boundary (class-validator / zod)
- All API routes protected by RBAC middleware
- Git conventional commits (feat:, fix:, docs:, test:, chore:)
- Tests colocated with source files (`*.spec.ts` / `*.test.ts`)

## Agent Team Roles

Full play-books in `md-files/agents/` (entry point: `agent-overview.md`). Routing rules in §3 below.

- **Team Lead** (`agent-team-lead.md`): Approval gates, RBAC sign-off, financial-non-negotiable enforcement
- **Architect** (`agent-architect.md`): Database schema, API contracts, system design decisions, ADRs
- **UX Designer** (`agent-ux-designer.md`): Flows, edge cases, destructive-action confirmation copy
- **UI Designer** (`agent-ui-designer.md`): PrimeReact composition, Tailwind tokens, screen inventory
- **Front-End** (`agent-frontend.md`): Next.js App Router, TanStack Query, routing, state
- **Back-End** (`agent-backend.md`): NestJS modules, Prisma, auth/RBAC, ledger writers, webhook handlers
- **QA/Testing** (`agent-qa-testing.md`): 5-test matrix per ledger handler, Playwright E2E, smoke gates
- **DevOps** (`agent-devops.md`): CI/CD, migrations, env validation, Docker/deployment

## Key Delivery Outputs

A) MVP backlog (epics, stories, tasks)
B) UX sitemap and flows
C) UI screen inventory + component list
D) Database schema (Prisma)
E) API contract with examples
F) Front-end routing and state strategy
G) Security checklist
H) Test plan + automation strategy
I) Deployment plan + runbook

---

## Current Implementation Status (2026-04-25)

HEAD: `41ec6d6` on main — **production cutover to socialbounty.cash complete**. End-to-end auth flow (Render API + Render web + Brevo SMTP) live and verified on `https://www.socialbounty.cash/login`. Single-rail TradeSafe cutover landed per ADR 0011; the historical Stitch / Stripe / Peach integrations were purged from code and docs in the prior wave. Test state: **~1280 tests across ~60 suites, 100% green** (estimate — backend agent 1A deleted ~371 legacy-provider tests, frontend 1C dropped ~4 tests, API count now ~976 / 60 suites + web 300 / 10 suites; Hard Rule #4 held). Destructive migration `20260424203941_drop_stitch_tables` dropped the legacy `stitch_payment_links`, `stitch_payouts`, `stitch_beneficiaries`, `stitch_subscriptions` tables; the `PayoutRail.STITCH` enum value is left in place (zero-risk, no rows reference it post-drop).

**Live and tested:**
- **Append-only ledger** — double-entry, integer minor units, plan snapshot per transaction, compensating-entry refunds (ADR 0005, 0006). Provider-agnostic; TradeSafe-driven postings route through the same `LedgerService.postTransactionGroup` as any other rail.
- **Reconciliation engine** — 7 checks (group balance, duplicate detection, missing legs, status consistency, wallet-projection drift, payouts-vs-ledger, reserve-vs-bounty). Reserve check runs single GROUP BY (184×–494× faster than per-bounty aggregate). 15-min cadence safe to ~1M paid bounties. Fault-injection coverage for each check. `checkPayoutsVsLedger` is provider-aware via the `PayoutRail` enum — post-cutover, only the `TRADESAFE` branch is exercised.
- **Finance admin dashboard** — kill switch, reconciliation drill-down, exception review, per-system confidence scores. Stat-card response key aligned with the Organisation→Brand rename (`data.brands.total`, fixed 2026-04-15). `getDashboard()` collapsed from 23 parallel `count()` round-trips to 6 `groupBy` queries (2026-04-16, commit `6e110ca`); response shape byte-identical, ~100-150ms → ~30-50ms dashboard load. Transaction-group drill-down audit log rendered a wrong shape (local type declared `auditLog` as array; backend returns single-or-null); swapped to shared `TransactionGroupDetail` and fixed consumer page (commit `2eb7a0a`).
- **KB automation (Phase 4)** — `recordRecurrence` signature-stable and called from reconciliation + webhook-failure paths, Ineffective-Fix auto-flag with AuditLog, `scripts/kb-context.ts` CLI.
- **Migration history reconciled** (batch 13A) — fresh-DB `prisma migrate deploy` green from empty Postgres; 16 tables, 16 enums, 23 columns, 56 indexes, 22 FKs brought back into migration history via idempotent SQL; `schema.prisma` untouched. **Correction (2026-04-17):** the "existing envs verified no-op via pg_dump snapshot diff" claim held for the ad-hoc env the audit ran against, but the staging Supabase DB had six unapplied migrations from this batch (13A) onward — see the 2026-04-17 "Migrations deployed + ledger idempotency hardening" entry below for the remediation.
- **Env validation hardened** (batches 13B + 14A) — 9 previously-unchecked flags now typed at boot; `BENEFICIARY_ENC_KEY` required when `PAYOUTS_ENABLED=true` with 32-char minimum + defence-in-depth throw in `BeneficiaryService` constructor; dead `FINANCIAL_KILL_SWITCH` env removed (operator-misleading; real kill switch is `SystemSetting.financial.kill_switch.active`).
- **Brand route consolidation** (2026-04-16, commit `8e4c21f`) — completed the half-done Organisation→Brand rename. `edit/`, `kyb/`, `members/`, `subscription/` moved from `/business/organisation/*` to `/business/brands/*` via `git mv` (history preserved); `navigation.ts` hrefs repointed; stale "Organisation" breadcrumbs fixed; redirect shim deleted. `/business/brands/edit/` (single-brand, `user.brandId`) and `/business/brands/[id]/edit/` (multi-brand, URL param) coexist intentionally.
- **Bounty form proof-requirements integrity** (2026-04-16, commit `f5353d2`) — a partially-landed refactor left four inconsistencies: `INITIAL_FORM_STATE.proofRequirements` seeded with `['url']` (drafts carried a value the user never selected), `buildCreateBountyRequest` defaulted empty arrays to the literal string `'url'` (draft + full), `isSectionComplete('bountyRules')` hardcoded `return true`, and `validateFull` never checked `proofRequirements`. All four fixed; UX change: the "URL" proof checkbox no longer pre-ticks — brands must explicitly select at least one proof type.
- **Web jest unblocked under TS 6.0** (2026-04-16, commit `a1527ff`) — `apps/web/tsconfig.json` missing `rootDir` (TS5011 promoted to error in 6.0) and ts-jest hard-codes `moduleResolution: Node10` (TS5107 deprecation). Added `rootDir: "./src"` + `ignoreDeprecations: "6.0"`. Web suite went from 8/8 fail at type-check to 8/8 green.
- **Canonical design system** (2026-04-18, branch `ui-ds-update`) — `apps/web/src/styles/design-system/` now holds the single source of truth (Claude Design handoff bundle). Includes `colors_and_type.css` (tokens), `components.css` (btn/card/input/badge/chip/avatar/table/toast/progress), `README.md` (voice + rules), `SKILL.md` (short agent reference), 5 preview HTMLs, and the wordmark asset. `globals.css` `@import`s both CSS files before `@tailwind`; `tailwind.config.ts` re-exports the canonical `pink-*` / `blue-*` / `slate-*` / `reward-*` scales. Identity: **pink leads (`pink-600` #db2777), blue counters (gradient stop + `.info` only), slate carries everything, gold is rewards-only, the `pink → blue` gradient is used ONCE per view at most.** Previous docs at `md-files/DESIGN-SYSTEM.md`, `docs/brand/WEBSITE-STYLING.md`, `docs/brand/BRAND-GUIDELINES.md` removed — their content is superseded by the handoff README.
- **Design system applied to all high-visibility pages** (2026-04-18, branch `ui-ds-apply`, merged to main) — followed the DS handoff with a full icon migration and DS-compliance sweep across all three surfaces (marketing/auth, participant, business + admin) via three isolated-worktree agents plus an integration pass. Scope and outcomes: **(1) PrimeIcons → Lucide everywhere** — `grep -r "pi pi-" apps/web/src --include="*.tsx" | wc -l` went from ~300 references to 0. `lucide-react@1.8.0` added as a dep; `NavItem.icon:string` retyped to `NavItem.Icon:LucideIcon`; `AppSidebar`, `AppHeader`, `BrandSelector` fully rewritten to Lucide. Social brand marks (Instagram, Facebook, Twitter — absent from Lucide 1.8 per trademark policy) replaced with neutral semantic stand-ins: `Camera`, `ThumbsUp`, `Globe`. `ICONS.md` corrected accordingly. **(2) Standalone blue-600 removed** from non-gradient non-info surfaces; replaced with `pink-600` (interactive) or `slate-600` (neutral). **(3) Off-spec Tailwind primitives** (`text-cyan-400`, `text-violet-400`, `text-amber-400`) eliminated — `grep -r "text-cyan-\|text-violet-\|text-amber-" apps/web/src --include="*.tsx" | wc -l` → 0. **(4) Metric/currency displays** → `font-mono tabular-nums` or `.metric` class. **(5) Eyebrow pattern** added to stat sections and page-header subtitle pairs. **(6) Card radii** normalised: `rounded-xl` default (16px), `rounded-3xl` feature (24px), `rounded-full` chips/CTAs. **(7) Gradient capped at one per view** across all three surfaces. **(8) Voice pass**: button labels trimmed, feature-lead copy rewritten receipts-first. Built as: 0c shared-component baseline commit → 3 parallel worktree agents (A: marketing+auth, B: participant, C: business+admin) → 12 cherry-picks onto `ui-ds-apply` with zero conflicts → integration fixes (32 files, local `LucideIcon` type aliases resolved, ts-jest TS5107 suppression added to `jest.config.ts`). Tests stayed at 1651 green (api 1347/87 + web 304/10). `next build --no-lint` clean (68 static pages).
- **`EmptyState.tsx` Lucide migration** (2026-04-19, commit `7dce09d`) — closed the last deferred item from `ui-ds-apply`. Component API changed from `icon?:string` (PrimeIcons class suffix) + `ctaIcon?:string` to `Icon?:LucideIcon` + `CtaIcon?:LucideIcon` (Lucide React component references passed by reference). Default changed from the string `'pi-inbox'` to the `Inbox` component import. Updated 27 call-sites across participant, business, and admin surfaces. `grep -r "pi pi-" apps/web/src --include="*.tsx" | wc -l` → 0 globally (was previously 0 from `ui-ds-apply` except these empty-state callers). Tests: 304/10 web green; `next build --no-lint` clean.
- **Organisation→Brand rename completed end-to-end** (2026-04-17, commits `c055b2a`, `55cb3b8`, `cb388a2`) — three-stage sweep closing the trailing half of the `8e4c21f` route consolidation. (B1) `apps/api/src/modules/organisations/` and its five files + `apps/web/src/app/(shared)/create-organisation/` renamed via `git mv` (history preserved); all imports repointed. (B2) 35 files, 157+/157- identifier sweep: `orgId` params → `brandId`, `organisation` local vars → `brand`, response fields `organisation`/`organisationName` → `brand`/`brandName`, test descriptions, comments. (B3) API contract: `@Get('disputes/organisation')` → `/disputes/brand`, `@Query('orgId')` → `@Query('brandId')`; frontend hook `useOrgDisputes` → `useBrandDisputes`, query-key `disputes.forOrg` → `disputes.forBrand`. Closed a latent bug — the web client was already calling `/disputes/brand` and hitting 404s on the old route. Intentionally preserved: Prisma `@map`/`@@map` directives (8 sites, DB table/column names retained to avoid a migration), JWT compat shim at `jwt.strategy.ts:11,41-42` + `auth.service.ts:428-430` (commit `8c50d38`), historical migrations, archived/dated docs.
- **Dead-code hygiene sweep** (2026-04-17, commits `c5de929`, `62dbf74`) — removed `ApifyService.refreshForOrganisation` alias (scheduler already on `refreshForBrand`, zero remaining callers) and unused `SettingsService.getString`; simplified 4 `as unknown as { contentFormat? | instructionSteps? | payoutMethod? }` casts in `useCreateBountyForm.ts` that were dead weight (fields already typed on `BountyDetailResponse`); added explanatory comment above the intentional `exhaustive-deps` eslint-disable in inbox conversation page; refreshed stale filenames in `docs/architecture/frontend-strategy.md` (`OrgStatusActions` → `BrandStatusActions`, `useOrganisation` → `useBrand`, `useAdminOrgs` → `useAdminBrands`).
- **Bounty accessType wired into API responses** (2026-04-17, commit `da71b0a`) — latent bug closed. Prisma schema has `accessType BountyAccessType @default(PUBLIC)` on Bounty, `BountyAccessType` enum exported from shared, and four frontend callsites read the field via `as unknown as { accessType?: string }` + `?? PUBLIC` fallback — but neither the list nor detail response builder included it, so every `accessType === 'CLOSED'` branch was unreachable (notably the padlock badge on `BountyCard` and the routing gate on `/bounties/[id]/apply`). Added `accessType: BountyAccessType` (non-nullable) to `BountyListItem` + `BountyDetailResponse` DTOs, serialized `b.accessType` in both response builders, dropped all four casts + fallbacks. `bounty-access.service.ts:528` uses its own Prisma `select` and is unaffected. UI-verified end-to-end against the live staging DB (37 public demo bounties): list + detail responses carry the field on the wire, Open badge renders, `/bounties/:id/apply` correctly redirects PUBLIC bounties back to the detail page.
- **Error-typing + repo hygiene** (2026-04-17, commits `867f130`, `880e5ab`, `f0b0bb5`) — three small threads: (a) `catch (err: any)` → `catch (err)` + `instanceof Error` narrowing at 3 production sites (health-check link validator, payment webhook verifier [historical Stripe site, since removed], social-handle validator) — TS 4.4+ default; (b) `.gitignore` now covers `*.tsbuildinfo`, `apps/web/test-results/`, `apps/web/playwright-report/`, and the four stale artefacts that had slipped into `c055b2a` were untracked via `git rm --cached`; `md-files/RELEASE-NOTES.md:20` "an brand" → "a brand" grammar lag from an earlier org→brand pass; (c) two new jest assertions lock in the `accessType` wire contract (`baseBounty` fixture gains `accessType: PUBLIC`; list + detail specs each assert a CLOSED value round-trips) — test count 1453 → 1455. `f0b0bb5` adds `.claude/launch.json` so future sessions can spin up the web dev server via Claude Preview's `preview_start web` without reconfiguring.
- **Migrations deployed + ledger idempotency hardening** (2026-04-17) — surfaced by running the dev stack against staging: two latent issues co-living on the server. **(A) Six migrations were un-applied** on the staging Supabase DB (historical migration filenames, including the since-dropped `…190000_stitch_subscription_upgrade` — all Stitch tables were dropped 2026-04-24 per ADR 0011) — the ReconciliationScheduler was crashlooping on `recurring_issues.ineffectiveFix` missing. All six had idempotency guards (222 `IF NOT EXISTS` / `IF EXISTS` clauses total across the batch) except the historical subscription one, which created fresh objects that pre-check confirmed absent. `prisma migrate deploy` applied all six cleanly; Prisma `_prisma_migrations` now in sync with code. **(B) Ledger idempotency catch-inside-tx was broken under real Postgres** — `ledger.service.ts:runInTx` used `try { tx.create(...) } catch (P2002) { tx.findUnique(...) }`, which worked on mocked Prisma in tests but hit SQLSTATE `25P02` ("current transaction is aborted") on the real DB, crashlooping `ClearanceService`'s clearance-release cron. Refactored to a fast-path pre-check outside any tx (cheap read), then a safety-net catch after `$transaction` rollback for the rare race where another writer won between pre-check and insert. ADR 0005 pattern preserved — `(referenceId, actionType)` still the idempotency key. Tests refactored: mock prisma now includes outer `ledgerTransactionGroup.findUnique`; existing "idempotent on conflict" test split into "fast-path pre-check" + "concurrent-writer race wins" scenarios. Test count 1455 → 1456.
- **Hunter submission verification — per-format URL coverage + Apify post-scraping** (2026-04-18, branch `bounty-submission`, 5 commits, ~3000 LOC, plan at `~/.claude/plans/quizzical-growing-owl.md`) — extends the existing brand-profile Apify scraper to per-submission post-level scraping with a hard approval gate. Three locked product decisions shaped the design: **(1) per-format URL coverage** — if a bounty asks for `INSTAGRAM:[REEL,FEED_POST]` + `TIKTOK:[VIDEO_POST]` the hunter must submit 3 URLs (one per format), no extras; **(2) eligibility verified from cached `UserSocialHandle`** (refreshed at hunter login) — zero extra Apify cost; **(3) hard approval gate with per-URL retry** — brand can't approve until every URL is `VERIFIED`, failed URLs trigger a hunter resubmit flow that re-scrapes only the failed ones (verified scrapes cached). Built via 3-agent parallel implementation on isolated worktrees (PR 2/3/4 simultaneously), each cherry-picked back into the branch. Five PRs:
  1. **PR 1 — Schema + shared types + per-format coverage validator** (`88dffcf`). New `SubmissionUrlScrape` child table + `UrlScrapeStatus` enum (PENDING / IN_PROGRESS / VERIFIED / FAILED) + `PostFormat` enum (mirrors the shared TS enum). New shared types: `ProofLinkInput`, `ScrapedPostData`, `VerificationCheck`, `SubmissionUrlScrapeInfo`. Pure-function validator at `apps/api/src/modules/submissions/submission-coverage.validator.ts` — checks every required (channel, format) pair has a URL, hostname matches `CHANNEL_URL_PATTERNS`, path matches `FORMAT_URL_HINTS` where defined (Instagram `/reel/`, `/p/`, `/stories/`; TikTok `/video/`; Facebook intentionally pattern-free), no extras, no duplicates. Wired into `SubmissionsService.create` + `updateSubmission` before the `$transaction`. **BREAKING**: `CreateSubmissionRequest.proofLinks` shape changed from `string[]` to `ProofLinkInput[]` (required, not optional). Migration uses idempotent `IF NOT EXISTS` + `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN null` pattern from batch 13A.
  2. **PR 2 — Apify post-scraping + verification orchestration** (`6e94f58`). Three new `ApifyService` methods (`scrapeInstagramPosts` / `scrapeTiktokPosts` / `scrapeFacebookPosts`) that batch URLs per channel into a single actor call (cost optimization — `apify/instagram-scraper.directUrls`, `clockworks/tiktok-scraper.postURLs`, `apify/facebook-posts-scraper`). Three new mappers normalize per-actor output → shared `ScrapedPostData`. New `SubmissionScraperService` orchestrates: per-submission Redis lock (`apify:scrape-lock:submission:{id}`), cost guard (skip Apify entirely when bounty has no `engagementRequirements`, no `payoutMetrics`, `contentFormat === BOTH`, no meaningful `structuredEligibility`), filter to PENDING-only URLs (cache VERIFIED scrapes across resubmits), eligibility lookup from `UserSocialHandle`, batched per-channel actor calls in parallel, verification computation, atomic upsert of all results. Pure `compute-verification-checks.ts` extracted for unit testability — covers 10 rule types: `tagAccount`, `mention`, `minViews`/`minLikes`/`minComments`, `contentFormat`, `formatMatch`, `minFollowers`, `publicProfile`, `minAccountAgeDays`. Trigger fires via `setImmediate` after `$transaction` commits (matches existing `auth.service.ts:155-165` fire-and-forget pattern). `urlScrapes` eager-loaded in all five response builders (`createSubmission`, `listMySubmissions`, `listForBounty`, `findById`, `updateSubmission`). Hard approval gate in `SubmissionsService.review()` rejects `APPROVED` transition with `BadRequestException({details: [...]})` if any URL is not VERIFIED. P2025 (row deleted mid-scrape) caught and logged-only.
  3. **PR 3 — Per-format submit form + resubmit-only-failed UI** (`018524d`). Submit page rewritten — `formatPairs = useMemo(...)` derived from `bounty.channels` renders one `<VerifiedLinkInput>` per (channel, format), labelled e.g. "Instagram REEL URL *". No add/remove buttons (no extras). Client-side validation mirrors the backend (CHANNEL_URL_PATTERNS regex). Detects existing `NEEDS_MORE_INFO` submission with FAILED `urlScrapes` → switches to resubmit mode: VERIFIED rows render read-only with green ✓ + "Already verified" badge, FAILED rows render editable with the prior `errorMessage` shown above. Submit handler patches the full set; backend re-scrapes only PENDING/FAILED rows. `apps/web/src/lib/api/submissions.ts` updated to send `proofLinks` as a JSON blob in FormData (legacy `forEach append` pattern was incompatible with `ProofLinkInput[]`).
  4. **PR 4 — Brand verification panel + approval gate UX** (`d7f2752`). New `<VerificationReportPanel>` component renders nothing for empty `urlScrapes` (no-rule bounty), spinner + "Verifying social posts..." while any URL is PENDING/IN_PROGRESS, then per-URL `glass-card` panels with status badge (emerald VERIFIED / rose FAILED), channel + format label, scraped metrics row compared side-by-side with `submission.reportedMetrics` (hunter claimed vs scraped), per-rule check list with `pi-check-circle` / `pi-times-circle` + required vs actual values. Failed URLs surface `errorMessage` in a red-tinted box. `<ReviewActionBar>` extended with `urlScrapes` prop — Approve disabled when `!allVerified` (empty array vacuously true so no-rule bounties stay approvable), tooltip explains why, "Needs More Info" reviewer note auto-prefilled with failure reasons (one-shot `useRef` flag prevents clobbering once user edits). New `useSubmissionWithPolling` hook in `apps/web/src/hooks/useSubmissions.ts` polls every 3s while any URL is in-flight, stops once all settled. Wired only on the brand review page; participant pages keep the static fetch.
  5. **Cleanup — redirect Update Submission button + delete redundant route** (`f8c601e`). The legacy `/my-submissions/[id]/update` page used the old flat `string[]` proofLinks shape and became dead code the moment PR 3's resubmit-only-failed flow landed on the per-format submit page. Deleted the route; the "Update Submission" button on `/my-submissions/[id]` now routes to `/bounties/{bountyId}/submit` (which auto-detects NEEDS_MORE_INFO state and switches into resubmit mode). `.gitignore` gains `.claude/scheduled_tasks.lock` + `.claude/worktrees/` to keep Claude Code session artefacts out of git.

  **Phase 2 deferred** (not blocking — flagged in plan): PostVisibility re-checks via scheduled re-scrape (`MUST_NOT_REMOVE` / `MINIMUM_DURATION` enforcement after deadline — needs new cron + history table); bounty-detail "rules to be auto-verified" preview (same `<VerificationReportPanel>` shape rendered with `actual: null` so brands see the assist before any submissions); stuck-PENDING recovery sweep (handles `setImmediate` lost across process restart). Costs: per-channel batching keeps the bill linear in submissions, not URLs; cost-guard zeroes the bill for no-rule bounties; per-bounty caps deferred until real-spend data justifies them. Risks documented inline in the plan file.

- **Hunter submission verification — Phase 2 follow-ups (visibility re-check + preview + recovery)** (2026-04-18, branch `phase2-postvisibility-recheck`, 7 commits, ~1900 LOC, all three Phase-2 deferred items shipped in one parallel-agent pass) — the three follow-ups flagged in `~/.claude/plans/quizzical-growing-owl.md` were spec'd into self-contained briefs and built simultaneously by 3 worktree-isolated agents (same pattern as Phase 1's PR 2/3/4), then cherry-picked back. One conflict on `submissions.module.ts` (both 2A and 2C added providers — trivial merge). Three threads:
  1. **Phase 2A — PostVisibility re-check scheduled cron** (`f24bde6`, `3be5582`, `5e8ec37`). New `submission-visibility.scheduler.ts` runs `@Cron(EVERY_6_HOURS)` with Redis pass-lock (`apify:scheduler-lock:visibility-recheck`). Cadence buckets: every 24h days 1–7 since approval, every 72h days 8–30, every 7d days 31+; capped at 90d for `MUST_NOT_REMOVE`, capped at `approvedAt + minimumDurationDays` for `MINIMUM_DURATION`. `MAX_PER_RUN = 100` ordered by oldest `lastVisibilityCheckAt` first to avoid starvation. New `SubmissionScraperService.rescrapeForVisibility(submissionId)` snapshots prior state to `SubmissionUrlScrapeHistory` (new append-only table), resets PENDING, calls existing `scrapeAndVerify`, increments `consecutiveVisibilityFailures` on failure or resets on success. Two-failures-in-a-row threshold triggers `RefundService.requestAfterApproval(submissionId, 'Auto-detected: post no longer accessible after N consecutive scrape failures', systemUser)` AND brand+hunter notification emails (new `post-removed-brand.hbs` + `post-removed-hunter.hbs`). First failure logs + `KbService.recordRecurrence({ category: 'post_visibility', system: 'submission-scraper', severity: 'medium', errorCode: 'POST_VISIBILITY_FIRST_FAILURE' })` so admins can see emerging issues. Migration adds 3 columns to `submissions` (`approvedAt`, `lastVisibilityCheckAt`, `consecutiveVisibilityFailures`) plus the new history table — idempotent `IF NOT EXISTS` throughout. New `AUDIT_ACTIONS.SUBMISSION_VISIBILITY_AUTO_REFUND` constant. SubmissionDetailResponse extended with the 3 new fields, `lastVisibilityCheckAt`/`consecutiveVisibilityFailures` SUPER_ADMIN-gated in the response builder. +10 tests covering lock contention, no-eligible-no-op, single-submission re-scrape, second-failure refund trigger, MUST_NOT_REMOVE 90d cap, MINIMUM_DURATION cap, MAX_PER_RUN honored.
  2. **Phase 2B — Bounty-detail "rules to be auto-verified" preview** (`264ae71`, `0251ca5`, `a60342f`). New `apps/web/src/lib/utils/bounty-preview-checks.ts` pure function `derivePreviewChecks(bounty)` returns `Record<string, VerificationCheck[]>` keyed by `${channel}_${format}` plus a synthetic `__eligibility__` group. Mirrors `compute-verification-checks.ts` rule emission but with `actual: null`. `<VerificationReportPanel>` extended via discriminated-union props (`SubmissionPanelProps | PreviewPanelProps`) — no breaking change for the existing brand-review consumer. Preview branch renders per-rule rows with `pi-shield text-accent-cyan` icons (no pass/fail since `actual` is null), eligibility group rendered above per-URL groups when present, headings swap by `audience` ("Auto-verification preview" for brands / "What you'll need to pass" for hunters; eligibility group title swaps to "Hunter eligibility" / "About you"). Empty-state collapse when bounty has zero rules — "No auto-verification rules — submissions go straight to manual review." Wired into both `apps/web/src/app/business/bounties/[id]/page.tsx` and `apps/web/src/app/(participant)/bounties/[id]/page.tsx`. +25 tests across 5 describe blocks (empty bounty, eligibility-only, all-10-rules, partial rules, multi-channel coverage).
  3. **Phase 2C — Stuck-PENDING scrape recovery sweep** (`920e605`). New `submission-scrape-recovery.scheduler.ts` runs `@Cron(EVERY_5_MINUTES)` with Redis pass-lock (`apify:scheduler-lock:scrape-recovery`, 4-min TTL — shorter than the 5-min cadence so the next tick can claim it). Queries `WHERE scrapeStatus = 'PENDING' AND createdAt < now() - interval '5 minutes'`, distinct by `submissionId`, `MAX_PER_RUN = 50`, ordered oldest-first. Calls existing `SubmissionScraperService.scrapeAndVerify` via `Promise.allSettled` (per-submission lock + idempotent PENDING-only filter handle the contention case). Lock-release `redis.del()` wrapped in `.catch()` so transient Redis blips can't crash the cron; lock NOT released when not acquired. +6 tests: lock contention, empty result, single dispatch, multi-dispatch with intra-submission distinct dedup, isolated scraper rejection containment, 5-minute cutoff filter.

  **Two infra gotchas surfaced during integration** (worth knowing for future cherry-pick passes): (a) ~~`npm run db:generate` writes only into `packages/prisma/node_modules/.prisma/client/` — `apps/api/node_modules/.prisma/client/` is independent and stale until `rsync -a --delete …`~~ — **resolved 2026-04-25**: root `db:generate` now chains the rsync, so `npm run db:generate` keeps `apps/api/node_modules/.prisma/client/` in sync automatically (local + CI). (b) `packages/shared` must be rebuilt (`npm run build:shared`) after any constants/DTO addition or api tests fail with "Property 'X' does not exist on type" against the stale `dist/` — affected `AUDIT_ACTIONS.SUBMISSION_VISIBILITY_AUTO_REFUND` here.

  **Still deferred** (out of scope for this pass): admin-facing visibility-failure surface (Phase 3 — would render `consecutiveVisibilityFailures` and history rows in the existing finance/KB dashboards); bounty-form preview during creation (separate UX decision — preview only renders on the saved bounty detail page for now); per-bounty/per-hunter cost caps on the visibility scheduler (defer until real-spend data justifies them).

- **Hunter submission verification — Phase 3 hardening + admin surface + analytics + ADR 0010** (2026-04-18, branch `phase3-visibility-hardening`, 15 commits, ~3300 LOC, all three Phase-2 deferrals + ADR-worthy auto-refund policy gap closed in one parallel-agent pass + one direct ADR write) — addressed the genuinely consequential gap that Phase 2A's auto-refund created (platform spending money based on Apify scrapes without a human in the loop) plus the deferred admin/preview/analytics surfaces. Built via 4 worktree-isolated agents simultaneously (3A, 3B, 3C, 3D) while I wrote the ADR. One conflict at integration on `submissions.module.ts` (already resolved in Phase 2 pattern) plus several additive merges in `finance-admin.service.ts` / `finance-admin.controller.ts` / `admin.dto.ts` / `useFinanceAdmin.ts` between 3B and 3D (both extending the same finance-admin surface — all additive, resolved by keeping both blocks). Plus one fixture fix (`MOCK_BOUNTIES.accessType`) chipped during the 3C work and merged inline. Five threads:
  1. **ADR 0010 — auto-refund-on-PostVisibility-failure** (`b7f5e86`). Documents the policy: 2-failure threshold (one failure could be Apify hiccup; two across a ≥6h gap is real signal), first-failure observable but not actionable (KB recurrence + hunter-only warning email; brand stays quiet), second-failure triggers `RefundService.requestAfterApproval` + brand+hunter notification + audit log + counter reset, kill-switch is absolute and fail-closed (`isKillSwitchActive().catch(true)` so a transient DB read failure biases toward not moving money), per-bounty cost cap of 30 re-scrapes lifetime, audit-trail contract (LedgerTransactionGroup + AuditLog + history row + KB recurrence), explicit risks including "Apify systemic failure causes mass false refunds" with the manual kill-switch + Phase 3D analytics as mitigation. Honours ADR 0006 (the visibility scheduler is NOT added to the kill-switch bypass list).
  2. **Phase 3A — visibility scheduler hardening** (`d296004`, `771cadb`, `5c1bcd9`, `5a7c73c`). Three changes to `submission-visibility.scheduler.ts` + supporting code. (i) **Kill-switch gate**: `LedgerService.isKillSwitchActive()` consulted only when about to refund (skip the per-tick DB hit on healthy submissions); on active → refund deferred, counter held at `previousConsecutive` (typically 1) NOT pinned at 2 so the row never looks post-hoc like 3+ failures occurred. Read failure caught and treated as active. (ii) **First-failure hunter warning email**: new `post-visibility-warning-hunter.hbs` template + `MailService.sendPostVisibilityWarningHunterEmail` method, fired on the 0→1 counter transition, never on 1→2 (the refund email handles that one), wrapped fire-and-forget try/catch. (iii) **Per-bounty cost cap**: `MAX_VISIBILITY_RESCRAPES_PER_BOUNTY = 30` lifetime, counted across all submissions of the bounty by `SubmissionUrlScrapeHistory` rows; one aggregate query per tick (no N+1), once-per-bounty log + KB recurrence (`VISIBILITY_RESCRAPE_CAP_REACHED`). +8 tests across 3 new describe blocks.
  3. **Phase 3B — admin visibility-failure surface** (`248bd73`, `778978a`, `79c05f0`, `56b65c8`). Two new SUPER_ADMIN-gated endpoints: `GET /admin/finance/visibility-failures` (paginated list of submissions with `consecutiveVisibilityFailures > 0`, ordered by failure-count desc; one Prisma query with includes, no N+1) and `GET /admin/finance/visibility-failures/:submissionId/history` (per-submission `SubmissionUrlScrapeHistory` rows newest-first). New DTOs `VisibilityFailureRow` + `VisibilityHistoryRow` + `AdminVisibilityFailureListResponse`. Frontend: new `/admin/finance/visibility-failures` page with PrimeReact DataTable (Bounty / Brand / Hunter / Approved at / Last checked / Failures Tag colour-coded emerald-amber-rose / Latest error truncated / History "View (N)" button → 90vw modal with per-history-row card showing channel + URL + status + scraped metrics + error message in rose-tinted box). Top-of-page banner reads open KB issues with `category: 'post_visibility'` and shows critical-severity ones in a red alert. New "Visibility Failures" nav entry slotted between Refunds and Exceptions. Admin submission detail page (`/admin/submissions/[id]`) gains a conditional "Visibility check status" panel when `consecutiveVisibilityFailures > 0` with explicit "Threshold reached — auto-refund will be issued by next visibility scheduler tick (ADR 0010)" red callout when count ≥ 2 + link to the new failures page. +19 api tests; no new web jest (page follows existing finance-pages integration-only convention).
  4. **Phase 3C — bounty form auto-verification preview during creation** (`2208046`, `0ab6c68`). `derivePreviewChecks` refactored to accept a smaller `BountyPreviewInput` slice (so both `BountyDetailResponse` from saved bounties and `BountyFormState` from the in-progress form satisfy it). `<VerificationReportPanel>` `audience` enum widened to add `'brand-form'` for context-appropriate empty-state copy ("Nothing to auto-verify yet — Select channels and add rules to preview"). New `<AutoVerifyPreviewAccordion>` collapsible (closed by default, `pi-shield` icon, sits under Section 3 Bounty Rules in the form) renders the panel live as the brand types — no extra API call, derives from form state. Mobile-tight design tokens. +13 web tests + 1 new test suite.
  5. **Phase 3D — visibility analytics** (`f010a8f`, `296f6f2`, `a341049`). New `GET /admin/finance/visibility-analytics?windowHours=24` endpoint backed by single `prisma.$queryRaw` join across `submission_url_scrape_histories` and `submission_url_scrapes`, grouped by `(channel, scrapeStatus)`. PENDING/IN_PROGRESS rows count toward `total` but NOT toward the failureRate denominator (transient rows would otherwise drag the rate down between scheduler ticks). Two-tier alert thresholds with sample-size floors: warning at `failureRate ≥ 0.30 AND total ≥ 10`, critical at `≥ 0.50 AND total ≥ 20` — tiny-sample noise (a 2/4 split) never trips a critical alert. New DTOs `VisibilityFailureBucket` + `VisibilityAnalyticsAlert` + `VisibilityAnalyticsResponse`. Frontend: new "Visibility Failure Rate (24h)" section on `/admin/finance/insights` above the existing per-system grid, per-channel DataTable with rate column emerald `<10%` / amber `10–30%` / rose `>30%`, PrimeReact `<Message>` alerts above the table when service emits any. Polls every 30s. +16 api tests covering empty window, all-VERIFIED, mixed rate, transient-row exclusion, warning + critical thresholds, sub-floor false-positive suppression, multi-channel isolation, totals roll-up, controller clamp + NaN fallback.

  **Plus one inline cleanup** (`cdc0351`): `MOCK_BOUNTIES` fixture in `apps/web/src/app/admin/component-library/_components/sections/OrganismsSection.tsx` was missing the `accessType: BountyAccessType.PUBLIC` field that landed in commit `da71b0a` (2026-04-17). Pre-existing `next build` failure spotted by Phase 3C's agent; fixed inline as part of the close-out.

  **Smoke-checked**: API boot to module-init phase confirmed clean DI for `SubmissionVisibilityScheduler` (with new `LedgerService` dependency), `SubmissionScrapeRecoveryScheduler`, and the three new admin routes — `/admin/finance/visibility-failures`, `/admin/finance/visibility-failures/:submissionId/history`, `/admin/finance/visibility-analytics` — all registered cleanly. Boot crashed on EADDRINUSE because there's a 2h-old API instance still on port 3001 (user's dev session); not a Phase 3 issue, just means user needs to restart their dev API to pick up the new code.

  **Still deferred for Phase 4+**: pre-refund "is this a global outage?" check inside the scheduler (cross-submission failure-rate aggregation distinct from Phase 3D's admin-facing analytics — defer until real-incident data shows a need); per-hunter rate limit on auto-refunds (defer until pattern data exists); refund-decision human review queue (commercial-pressure-driven, behind a feature flag if added); KB analytics for non-visibility categories on the same insights surface.

- **Mobile UX pass + bounty form polish** (2026-04-17, branch `hunter-profile-update` → merged in `f827879`, 26 commits, 37 files, +885/-391) — comprehensive bounty-form rework driven by hands-on mobile testing. Five threads:
  1. **Component-library mobile tightening** (`844bec7`, `b86282a`) — 8 components shrunk for mobile (text + spacing scale one step down at `<sm:`, padding tightened, no h-scroll). Historical Mobile Tightening Checklist (originally `DESIGN-SYSTEM.md §10`, now superseded by the Claude Design handoff bundle at `apps/web/src/styles/design-system/`).
  2. **PrimeReact theme fixes** — Dropdown double-border (cascading global `.p-inputtext` border was painting an inner border inside the wrapper, fixed in `af4b7a8`); single-line form-controls locked to 2.5rem (40px) — now `min-height: 2.5rem` in `components.css:.input` (d19db0d); Toast width constrained to viewport on mobile to stop right-edge overflow (`fae4cf8`).
  3. **Bounty-form UX** — multiple coordinated changes. Toggle-row inputs stack on mobile (`559f268`); Access Type options stack on mobile (`9d4ba79`); brand-asset filename overflow fixed (`0016bca`); Cancel button removed from footer (`3b588ca`); footer fully reshaped — label-left/amount-right, single-row mobile layout ~52px tall, dropped "N/M complete" duplicate progress indicator (`9025a3a`, `d6faa9b`, `4b68f4f`, `9ba7b11`, `0f121b2`); section-status icons → Required / Optional / Complete pills with `optional?: boolean` + `hasContent?: boolean` props on `SectionPanel` (`74798d0`, `279682a`); Required `*` markers added to every submission-required field (`0d3204f`); CASH rewards skip description input (auto-fill `name='Cash'` in reducer, hide UI, skip name validation) (`adb1ec8`); TikTok auto-selects Video Only and disables Photo Only when picked (`b9b8653`); Duration + Unit inputs split 50/50 on mobile (`e5711ba`); fixed-footer pads form `pb-[calc(...)]` with `env(safe-area-inset-bottom)` (`869c9df`).
  4. **Post-visibility acknowledgment gate removed end-to-end** (`29a3b72`) — frontend toggle, state, action, props all dropped; backend `bounties.service.ts:1053` (DRAFT→LIVE precondition) and `:866` (reset-on-update) gates removed; 5 backend tests rewritten/deleted (VE-21, VE-35, IS-08, two `create-bounty-edge-cases` cases) with `.not.toContain('visibilityAcknowledged')` + `.not.toHaveProperty('visibilityAcknowledged')` regression locks; the `acknowledgeVisibility` controller route + service method kept as documented no-ops for backward compat; column kept on `Bounty` model for historical rows (no destructive migration). All 4 spec docs (`create-bounty-spec.md`, `create-bounty-tests.md`, `create-bounty-ui.md`, `create-bounty-ux.md`) carry `DEPRECATED 2026-04-17` markers.
  5. **Payments — Create-to-checkout shortcut** (`bd2480b`, `0cdb5bb`, `cc46f2f`) — Create Bounty button on the new-bounty form chains directly into `bountyApi.fundBounty()` and the hosted-checkout redirect (was: navigate to detail page, find "Go Live", trigger payment). Same wiring on the edit page for unpaid DRAFTs (`bounty.status === DRAFT && paymentStatus !== PAID`). New `apps/web/src/lib/utils/redirect-to-hosted-checkout.ts` helper centralises the redirect: production uses `window.location.href` (standard hosted-checkout pattern); dev/preview uses `window.open(_blank)` so iframe-sandboxed previews (Claude Preview, staging tools) can exercise the flow without same-page navigation being blocked. Wired into all 4 redirect call-sites. (Post-2026-04-24 ADR 0011 cutover: hosted checkout is now TradeSafe's; the redirect plumbing is unchanged.)
  6. **Bounty Rules pill correction + form-render fix** — Bounty Rules was showing "Complete" on page-load because `proofRequirements` is auto-seeded `['url']` to match the inline "post links required" notice. Marked the section `optional` with `bountyRulesHasContent(state)` helper (eligibility filters, engagement requirements, post visibility, max submissions) so the pill reads "Optional" until the brand engages.

- **Legal document wiki** (2026-04-24, commit `6cabc15`, +6427/-321 across 23 files) — single pre-launch surface for unauthenticated visitors at `/legal` covering all SA-law-mandatory policies. Eleven first-draft documents across four categories: **POPIA & Data** (Privacy Policy, Cookie Policy, PAIA Manual, Information Officer & Data Subject Rights), **Commercial** (Terms of Service, Payout & Escrow Terms), **Consumer** (Consumer Rights Notice, Complaints & Dispute Resolution), **Platform Rules** (Acceptable Use, Disclaimer, IP & Copyright Takedown). Every doc imports entity facts from a single source of truth at `apps/web/src/content/legal/entity.ts` (`Social Bounty (Pty) Ltd`, CIPC `2026/301053/07`, registered office `2 Alyth Road, Forest Town, Johannesburg, 2193`, Gauteng High Court jurisdiction, currently not VAT-registered) and renders inside the shared `<LegalDocLayout>` at `apps/web/src/components/legal/LegalDocLayout.tsx` with breadcrumbs, a sticky desktop ToC (split into `LegalTableOfContentsDesktop` + `LegalTableOfContentsMobile` exports so Next.js server components can render the layout server-side while only the ToC stays client-interactive), and a red-tinted *"working draft — attorney review required"* banner on every page. **Legal gap was driven by the ADR 0011 TradeSafe-as-AGENT pivot** — the previous `/terms` claimed platform custody, which was never true post-0011; the new Terms of Service correctly describes TradeSafe Escrow (Pty) Ltd as the registered-escrow custodian. Cross-references inside docs use `<Link>` for SPA navigation; external partners (TradeSafe, Information Regulator, NCC, Apify) use `<a target="_blank" rel="noopener noreferrer">`. Legacy `/privacy` and `/terms` 308-redirect via `next.config.mjs` to `/legal/privacy-policy` and `/legal/terms-of-service` respectively — preserves any external links to the old URLs. Footer `FOOTER_COMPANY` array collapsed from `Pricing · Contact · Privacy · Terms` to `Pricing · Contact · Legal` (one door). Middleware `MARKETING_ROUTES` allowlist updated from `/privacy`, `/terms` to `/legal` (prefix-matches all sub-routes). Complementary founder-tracker at `docs/compliance/post-incorporation-checklist.md` flags: Public Officer appointment at SARS (deadline 2026-05-14), Beneficial Ownership filing at CIPC (overdue ~2026-04-28), Information Officer registration with IR, PAIA Manual IR filing, B-BBEE EME affidavit. Every draft carries attorney-review open items inline (AI-training opt-out default, ECTA §44(3)(b) cooling-off carve-out, voluntary-arbitration position per CPA §52, ZAR 10,000 liability cap calibration, 5y-vs-7y Tax Admin Act retention window, `abuse@socialbounty.cash` mailbox not yet provisioned, `[ANALYTICS_PROVIDER]` placeholder in Cookie Policy). Dev-only `web-preview` launch config added at `.claude/launch.json` (port 3010) so Claude Preview sessions can spin up the web app without clashing with a developer's running `:3000` instance.
- **Production cutover to socialbounty.cash** (2026-04-25, commits `d4f93f3`, `cb02de1`, `284f86a`, `945970d`, `41ec6d6`) — first end-to-end production deployment with auth flow live. Both API and web frontend now serve from Render Frankfurt (matching Supabase eu-west-3 region for ~150ms RTT to Johannesburg vs ~250ms from Oregon), with Brevo as the transactional SMTP relay. Five threads:

  1. **Two-service Render blueprint** (`d4f93f3`) — extended `render.yaml` with a second `social-bounty-web` service alongside the existing `social-bounty-api`. New `apps/web/Dockerfile` mirrors the API multi-stage pattern (`node:20-slim` builder → runtime, ~150MB final). Both services auto-deploy on push to main, both pinned to Frankfurt. API at `api.socialbounty.cash`, web at `www.socialbounty.cash` + apex `socialbounty.cash`. `app.socialbounty.cash` reserved for the future authenticated app subdomain (declared in `TRADESAFE_REDIRECT_URL` / `_SUCCESS_URL` / `_FAILURE_URL` but DNS not yet wired — not exercised under `TRADESAFE_MOCK=true`).

  2. **Web Dockerfile public/ COPY fix** (`cb02de1`) — first deploy crashed at runtime stage 10/12 because `apps/web/public/` doesn't exist in this monorepo. Next.js treats `public/` as optional, so removing the COPY unblocked the build (`next build --no-lint` confirms 68 static pages, no missing-asset warnings).

  3. **Marketing /pricing → /payment swap** (`284f86a`) — public-site rework centred on a new `/payment` page: TradeSafe escrow explainer, 4-step payment flow (brand funds → TradeSafe holds → brand approves → hunter EFT), worked R500 fee breakdown (brand pays R617.50, hunter earns R382.50 at Free-plan rates), payment methods grid, important-terms grid with deep links into `/legal`, FAQ. Pro-subscription cards rendered as "coming soon" with no pricing — per ADR 0011 single-rail. Old `/pricing` route deleted; 308 redirect in `next.config.mjs` preserves external links. Nav + footer labels swapped Pricing → Payment. `/payment` added to middleware `MARKETING_ROUTES` allowlist. `/join/hunter` + `/join/business` Pro cards flipped to coming-soon state (no R350/R950 prices, disabled CTA, "See how payments & fees work →" link to `/payment`). Terms of Service §8.3 softened: "Pro Hunter / Pro Brand are coming soon"; Free-plan rates explicitly in force until then.

  4. **NEXT_PUBLIC_API_URL build-time bake-in + CORS expansion** (`945970d`) — login on `https://www.socialbounty.cash/login` returned the catch-all "Something went wrong" because the deployed JS bundle had `http://localhost:3001/api/v1` literal-baked in. Root cause: `NEXT_PUBLIC_*` env vars are inlined into the Next.js bundle at `next build` time, **not read at runtime** — `render.yaml`'s runtime `envVars` block was too late, the bundle was already compiled with the localhost fallback from `client.ts`. Fix: `ARG NEXT_PUBLIC_API_URL=https://api.socialbounty.cash/api/v1` + matching `ENV` declared before `npm run build:web` in the web Dockerfile. ARG-with-default keeps per-env builds possible via `docker build --build-arg`. Confirmed against the live deploy by inspecting chunk `3596-*.js` on the wire — localhost URL gone after redeploy. Stacked second bug in the same commit: `CORS_ORIGIN` was scoped to `https://app.socialbounty.cash` only, but the live frontend serves at `www`/apex — every preflight would have been rejected even after the bundle fix. Expanded to comma-separated `https://www.socialbounty.cash,https://socialbounty.cash,https://app.socialbounty.cash` (parsed at `apps/api/src/main.ts:64`, supports comma-separated lists).

  5. **SMTP boot verification + Brevo integration live** (`41ec6d6`) — even after bundle + CORS fixes, OTP emails silently never arrived. Root cause: SMTP send is **fire-and-forget by design** (`auth.service.ts:123`, prevents email enumeration via response timing) and the transporter was constructed lazily — first failure happened at first send-time, never at boot. Added `transporter.verify()` call in `MailService.onModuleInit` (`apps/api/src/modules/mail/mail.service.ts:138-180`) which probes the SMTP transport at boot and logs structured success/failure with host, port, authenticated boolean. Non-blocking (no `await`) so a slow SMTP host can't delay app boot; non-fatal because dev environments routinely run with no SMTP (Mailpit at localhost:1025 default) and crashing boot would block local work. Surfaced the actual problem within seconds of deploy: `Invalid login: 535 5.7.8 Authentication failed` from Brevo — operator had pasted the v3 API key into `SMTP_PASS` instead of an SMTP key (different Brevo concept, generated in **SMTP & API → SMTP tab**, not the API Keys tab). After regenerating an SMTP key and re-pasting both `SMTP_USER` (the **Login** value shown in Brevo's SMTP tab — sometimes differs from account email) and `SMTP_PASS` in Render dashboard, boot probe flipped to `SMTP transport verified at boot { host: 'smtp-relay.brevo.com', port: 587, authenticated: true }`. End-to-end OTP flow now working on production.

  **Production env vars set in Render dashboard (`sync: false` in render.yaml):** `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `JWT_SECRET`, `JWT_REFRESH_SECRET`, `DATABASE_URL`, `DIRECT_URL`, `REDIS_HOST`, `REDIS_PORT`, `REDIS_PASSWORD`, `BENEFICIARY_ENC_KEY`, `APIFY_API_TOKEN`, `TRADESAFE_CLIENT_ID`, `TRADESAFE_CLIENT_SECRET`, `TRADESAFE_WEBHOOK_SECRET`, `TRADESAFE_CALLBACK_SECRET`, `SENTRY_DSN`, `SYSTEM_ACTOR_ID`. TradeSafe still in mock mode (`TRADESAFE_MOCK=true`, R24 is the sole gate to live money flow). SMS rail vars added in Wave 3M (branch `sms-login`): `SMS_ENABLED` (`false` until ZA alphanumeric sender ID is registered with Brevo), `BREVO_API_KEY` (Brevo REST API key — distinct from `SMTP_PASS` which is the SMTP credential), `BREVO_SMS_SENDER` (`SocBounty`), `BREVO_SMS_DEFAULT_REGION` (`ZA`).

  **Deferred for follow-up:**
  - **Brevo sender verification** for `noreply@socialbounty.cash` — SMTP auth works, but the send still requires either single-sender verification (Brevo dashboard → Senders & IPs → Senders) or full domain authentication (SPF/DKIM/DMARC at the `socialbounty.cash` registrar). Without it, Gmail/Outlook drop OTP emails to spam. Not blocking the auth flow itself but blocking deliverability.
  - **`app.socialbounty.cash` DNS** — declared in TradeSafe redirect/success/failure URLs but not yet CNAME-ed at the registrar. Needs to be wired before `PAYOUTS_ENABLED=true` flip.
  - **Domain authentication for `socialbounty.cash`** (SPF/DKIM/DMARC) — Brevo provides the exact records on dashboard once requested.

- **Multi-channel OTP login (Email + SMS via Brevo)** (2026-04-25, branch `sms-login`, ~3 commits) — added a sibling SMS rail to the existing email OTP. New `User.phoneNumber` (collected required at signup, verified on first SMS-OTP login). New `POST /auth/switch-otp-channel` (3/min throttle, max 2 switches per OTP record, cooldown exempt). Per-phone daily cap of 10 SMS / 86400s. Silent-on-missing-phone for the SMS path (anti-enumeration parity with email). New `SmsService` mirrors the `MailService` pattern (constructor + `onModuleInit` boot probe + fire-and-forget). Wave 0.5 live smoke confirmed Brevo accepts at HTTP 201 with sender=`SocBounty` to a ZA mobile (10.2 credits per SA SMS). All four critical auth events now AuditLog'd (`OTP_REQUESTED`, `OTP_VERIFIED_LOGIN`, `OTP_CHANNEL_SWITCHED`, `SIGNUP_COMPLETED`) — closes the prior zero-audit gap on the auth flow. Behind `SMS_ENABLED=false` in prod until alphanumeric sender ID is registered with Brevo for ZA. ADR 0012 captures the design.

- **Single-rail TradeSafe architecture** (2026-04-24, commit `<Wave 2 integration>`) — All historical Stitch, Stripe, and Peach Payments code deleted. Bounty funding now routes through `TradeSafePaymentsService.createBountyFunding` (Phase 3 inbound cutover — per ADR 0011). The `FUNDS_RECEIVED` TradeSafe webhook posts the `bounty_funded` ledger group and flips `Bounty.status: DRAFT → LIVE`. Refund flow ported from Stitch API calls to synchronous ledger compensating entries (ADR 0005/0006). Subscription upgrade backend fully removed; UI retains a "Pro subscriptions coming soon" placeholder — if Pro subs come back, they'll be built on a new rail. R24 (TradeSafe prod OAuth creds) remains the sole blocker to real-money flow — `TRADESAFE_MOCK=true` on Render until creds arrive. Destructive migration `20260424203941_drop_stitch_tables` dropped `stitch_payment_links`, `stitch_payouts`, `stitch_beneficiaries`, `stitch_subscriptions` tables; the `PayoutRail.STITCH` + `WebhookProvider.STITCH` enum values are left in place (zero-risk, historical `webhook_events` rows stay queryable). 13 env vars deleted (STITCH_*, STRIPE_*, SUBSCRIPTION_UPGRADE_ENABLED, PAYMENTS_PROVIDER). ~5100 LOC removed, ~1800 LOC added (including 12 new TradeSafe tests). Test count 1651 → ~1280 API + ~301 web.
- **Brand bounty dashboard + 5-step wizard + multi-claim escrow** (2026-04-28, branch `bounty-flow-refinement`, [PR #69](https://github.com/NicholasPaulCarl/social-bounty/pull/69), 9 commits, +3050/−501 across 37 files) — ADR-territory financial change plus a UX refinement of the brand bounty hub and the bounty creation flow. Five threads:
  1. **ADR 0013 — multi-claim bounty escrow** (`docs/adr/0013-multi-claim-bounty-escrow.md`). TradeSafe escrow formula at funding time changes from `sum(rewards)` to `sum(rewards) × maxSubmissions` so a bounty with `maxSubmissions = 10` can actually pay 10 hunters. Pre-ADR-0013 the brand_reserve hit zero on submission #2 and tripped `checkReserveVsBounty`. Forced sub-decisions: (a) `maxSubmissions` required at funding (precondition added to `tradesafe-payments.service.ts createBountyFunding` — throws `BadRequestException` if missing); (b) `maxSubmissions` immutable post-fund (dropped from `LIVE_EDITABLE_FIELDS` in `bounties.service.ts:54-58`, set is now `eligibilityRules`, `proofRequirements`, `endDate`); (c) capacity changes use existing pre-approval refund + recreate path — no new top-up rail; (d) forward-only cutover, existing rows stay self-consistent (their `faceValueCents` matches their ledger; new rule applies at funding time only — no data migration). `computeFaceValueCents(rewards, maxSubmissions)` now takes both args and returns `perClaim * BigInt(maxSubmissions)` to preserve Financial Non-Negotiable #4. All 10 NNs continue to hold; reconciliation, refund formula, and approval ledger all survive unchanged because they read `bounty.faceValueCents` (which now reflects the multiplied amount). 5-test matrix at `tradesafe-webhook.handler.funds-received.spec.ts` updated; new multiplier coverage tests added; the LIVE-edit `maxSubmissions` test in `update-bounty.service.spec.ts` was inverted to assert the post-ADR-0013 rejection.
  2. **Brand bounty dashboard hub** (`/business/bounties`). Quick Create card grid above the existing list — four cards (Blank · Social Exposure · Check-Ins · Product Sales). Presets navigate to `/business/bounties/new?preset=<id>`; preset registry at `apps/web/src/components/bounty-form/bounty-presets.ts` exports `BOUNTY_PRESETS`, `BountyPresetId`, `isBountyPresetId`, `getPresetFormState`. List rows now use a PrimeReact `Menu` (ellipsis trigger) instead of always-visible icons; grid mode keeps the icon footer. Edit visibility decided by `getManageMenuPolicy` (pure module at `apps/web/src/components/features/bounty/manage-menu-policy.ts`) — DRAFT always editable, LIVE/PAUSED editable only with zero submissions, CLOSED never. `BountyListItem.submissionCount` (already on the wire from `packages/shared/src/dto/bounty.dto.ts:138`) is the source of truth for the gate. `PAGE_LIMIT` changed 12 → 25.
  3. **5-step bounty creation wizard** (`apps/web/src/components/bounty-form/WizardShell.tsx`, new). Steps: Basics · Instructions & Metrics · Access & Requirements · Claim & Rewards · Document Share. The reducer in `useCreateBountyForm.ts` is unchanged — `WizardShell` is a pure-presentational scaffold over the existing `<SectionPanel>` blocks, gated by a local `currentStep` in `CreateBountyForm.tsx`. Step indicator is non-interactive (Back/Next is the only path forward, so per-step validation gates can't be skipped). Save Draft + Discard available at every step; Discard wraps a `ConfirmAction` dialog and calls a page-level `onDiscard` (routing decision lives at the page). Preset hydration via `searchParams.get('preset')` → `getPresetFormState(id)` → `useCreateBountyForm(initialBounty, initialFormOverride)` (edit mode trumps preset). New `validateStep(stepIdx, state)` helper in `validation.ts` filters `validateFull` by `WIZARD_STEP_SECTIONS[stepIdx]`, blocking forward navigation when the current step has errors; full validation still runs only on Create. `SECTIONS` const in `types.ts:179` fixed to include the missing `accessType` entry. Legacy `FormSummaryFooter.tsx` deleted — wizard footer renders the multiplied total readout directly; breakdown subline `× N claims` shown when `maxSubmissions > 1` (per ADR 0013 §"Risks" #1).
  4. **Frontend display contract** (per ADR 0013 §1). `useCreateBountyForm` exports two memoised values: `perClaimRewardValue = sum(rewards)` (what one approved hunter earns) and `totalRewardValue = perClaim × (maxSubmissions ?? 1)` (what's escrowed on TradeSafe). Pure helpers `computePerClaimRewardValue` + `computeTotalRewardValue` are exported for unit testing. Falls back to ×1 when `maxSubmissions` is null so the UI never renders NaN before the brand fills the field. Rendered in `RewardLinesSection` (per-claim + multiplier line + total) and in the wizard footer (multiplied total prominent, breakdown subline). Bounty detail pages (business + participant) display the same two numbers via the API response — `bounties.service.ts` private `computeTotalRewardValue(rewards, maxSubmissions)` mirrors the frontend math at four response builder sites. The API field name `BountyDetailResponse.totalRewardValue` is unchanged but its meaning shifted from "sum of rewards" to "committed bounty value" — list + detail consumers updated accordingly.
  5. **Brand asset upload limits 10/10MB → 5/20MB** + **`/payment` marketing copy** refreshed for the multiplier. `BRAND_ASSET_LIMITS.MAX_FILES_PER_BOUNTY` and `MAX_FILE_SIZE` in `packages/shared/src/constants.ts` updated; API validation pulls from shared (no hard-coded duplicates), boundary tests bumped. `/payment` worked-example scoped to a single-claim R500 bounty with explicit per-claim labels and a multiplication callout (5 claims = R3,087.50; 10 claims = R6,175.00); two FAQ entries added covering the multiplier and why `maxSubmissions` is frozen post-funding.

  Built as 4 parallel worktree-isolated agents (Waves A/B/C/D, matching the project's existing `Hard Rule #8` pattern — same as the hunter-submission-verification work) plus a single integration cherry-pick pass resolving two conflicts: `bounty-presets.ts` favoured Wave A's full registry over Wave B's stub, and `CreateBountyForm.tsx` kept Wave B's wizard structure with Wave D's three surgical display-prop additions folded in. Test state: **86 suites / 1682 passed / 3 skipped** (api 73 / 1321 + web 13 / 361). `next build --no-lint` clean (78 static pages). Visual QA on `/payment` confirmed live; the BUSINESS_ADMIN-gated dashboard + wizard flows could not be exercised in-session and are flagged in the PR for manual smoke before merge.

  **Cooldown follow-up (commit `5707463`, 2026-04-28)** — both wizard validation gaps flagged in the original PR's cooldown summary closed in one paired fix: (a) `validateFull` now requires `maxSubmissions` non-null with a clear "Number of claims is required" error (draft mode unchanged — drafts can save without it); (b) `getSectionErrors` moved `'maxSubmissions'` from `bountyRules` → `bountyContent` so the error surfaces on the wizard's step 3 panel (where `MaxSubmissionsSection` actually renders). Pre-fix path: brand fills wizard with `maxSubmissions = null` → wizard advances → Create → backend rejects at funding with a generic toast. Post-fix path: brand blocked on step 3 with an inline error pointing at the actual input. `validation.test.ts` updated — flipped the previously-passing null case, added a `'should bucket maxSubmissions under bountyContent'` test to lock the section-key contract, reduced the `bountyRules` count test from 4 → 3. Web jest 361 → 362 (one new test).

  **Wave 1 polish landed (2026-04-29, commits `2cf89e7`/`75a901a`/`695732f`/`a78b2d7`/`72a46bb`/`ba54308`)** — six parallel-dispatched polish items closing the gap between the dashboard/wizard briefs and the Claude Design handoff bundles. Plan at `docs/planning/2026-04-29-bounty-flow-polish-plan.md`. Six threads: (i) `TOGGLE_FORMAT` reducer auto-deletes the channel entry when the last format on a platform is unchecked (matching design's `if (list.length === 0) delete next[pid]`); (ii) Quick Create cards render lucide platform-icon chips beneath the description (`Camera`/`ThumbsUp`/`Video` per the `ui-ds-apply` stand-in convention) — `BountyPreset.platforms?` is the new display-only field, doesn't drive form state; (iii) `WizardShell` stepper pills are now clickable buttons — backward jumps free, forward jumps re-run `validateStep` via parent's `onJumpToStep`; the discard flow swapped from single-Confirm `ConfirmAction` to a 3-button PrimeReact `Dialog` (Keep editing · Save as draft · Discard); `CreateBountyForm` now tracks `completedSteps` state populated on every successful Next; (iv) Step 3 Location is hard-locked to "South Africa" — the free-text input was replaced by a slate-bordered locked display + lucide `Lock` icon + PrimeReact secondary `Tag`, click toggles a pink-700 hint; `INITIAL_FORM_STATE.structuredEligibility.locationRestriction` defaults to `'South Africa'` (was `null`); `bountyRulesHasContent` and the draft-mode `hasEligibility` guard both exclude the literal default so the section's "Optional" pill stays correct on fresh forms; (v) three-dot menu adds a Duplicate action (lucide `Copy`) on every status — `handleDuplicate` calls `bountyApi.getById` then `bountyApi.create` with a cloned payload (title prefixed `"Copy of "` with `BOUNTY_TITLE_MAX` truncation, `startDate` reset to null, no submissions/invitations carried over), toasts success, navigates to the new draft's edit page; the grid-mode `BountyManageActions` icon row gets the same Duplicate button; new `BountyManageRowMenu.test.ts` locks "Duplicate appears regardless of status" across DRAFT/LIVE-0/LIVE-with-subs/PAUSED/CLOSED; (vi) wizard auto-scrolls to top on every Next/Back transition (already in place pre-Wave 1 — Wave F was a no-op verify), and the hub gains an "All bounties" eyebrow above the status pills. Web jest 362 → 384 (+22 across the six items + cooldown's `WizardShell` docstring fix). Cherry-pick orchestration note for future dev-team-lead runs: of the 6 dispatched worktree-isolated agents, only B and D committed to their worktree branches as briefed; A/C/E/F committed directly to the parent branch (`bounty-flow-refinement`) — same end result but the `--theirs` resolution path was different (B/D needed `git cherry-pick` from worktrees, A/C/E/F were already on the branch). Brief future agents to verify with `git log -1` post-commit.

- **Bounty UX/UI design alignment** (2026-04-30, branch `bounty-ux-ui`, 7 commits + cooldown, plan at `docs/planning/2026-04-30-bounty-ux-ui-design-alignment-plan.md`) — second polish pass over the brand bounty surfaces against the same Claude Design handoff bundle. Wave 1 closed five items deferred from the prior pass: (i) **list view default** — `useManageFilters.ts` `DEFAULT_FILTERS.view` flipped `'grid'` → `'list'`, view toggle dropped from the page header (commit `ddd7057`); (ii) **bounty list table redesign** — `BusinessBountyListView` now renders 8 columns matching the design (Bounty name+id / Category / Platforms chips / Status badge with colored dot / Reward / Claims progress bar / Ends / ⋯ menu); new `PlatformChips` helper component, +14 tests pinning column rendering + status colour matrix + null-maxSubmissions edge case (commit `92d7cd8`); (iii) **drag-to-reorder Step 2 instructions** — `REORDER_INSTRUCTION_STEP` reducer action (with from/to bounds-guard), HTML5 drag-and-drop with `GripVertical` handle on each row; iOS Safari touch is a known platform limitation (HTML5 `draggable` doesn't fire on touch — accepted, no library swap); +6 reducer tests (commit `f5b0ea1`); (iv) **sticky KPI sidebar on Step 4** — `RewardLinesSection` restructured to `grid-cols-1 lg:grid-cols-[1fr_300px]`; new `RewardCalculator` presentational component renders prominent total + per-claim/claims/type breakdown with `lg:sticky lg:top-24`; +7 tests (commit `64987f4`); (v) **per-slot upload progress on Step 5** — `BrandAssetsSection` restructured to slot-based UI; `runSimulatedProgress` exported helper (140ms tick, 12-30% bump per tick, clamps to 100); cosmetic feedback only — slow-network real-upload may complete after the bar visually fills (documented limitation); +7 tests (commit `79a215b`). Wave 2 consolidated **hub layout polish** in one commit (`a6ae919`): three new components — `BountyHubHeader` (replaces the gradient `ManageHero` with plain H1 + subtitle + primary CTA), `BountyStatusSegmented` (replaces `BountyStatusPills` with a 4-tab segmented control; PAUSED dropped from visible UI but still URL-routable via `?status=PAUSED`), `BountyFilterRow` (replaces `BrowseFilterBar` on the brand hub with search + stub Filter button; reward filter + sort dropdowns dropped — URL keys still round-trip if set externally). The `BrowseFilterBar` shared component stays untouched (still consumed by `(participant)/bounties` hunter browse). Cooldown deleted three orphaned components (`ManageHero.tsx`, `BountyStatusPills.tsx`, `BountyManageCard.tsx`) plus stale doc-comment refs. Web jest 411 → 432 (+21 across the three new hub components) → 432 post-cooldown. Plus the same orchestration anomaly as the prior pass: 3 of 5 Wave 1 agents committed directly to the parent branch (`bounty-ux-ui`) instead of their worktree branches; only A and E committed to worktree branches as briefed (cherry-picked back). Future dev-team-lead briefs will continue to require `git log -1` post-commit verification — until the agent-tooling guarantees worktree isolation, parent-branch commits remain a recurring failure mode that costs an extra integration step.

- **TradeSafe FUNDS_RECEIVED wiring fix + cooldown polish** (2026-04-27, commits `72d00b7` + `<this commit>`) — closed a latent bug from the Phase 3 cutover and two cooldown-pass corrections. **Wiring fix**: the URL-secret-protected callback at `POST /webhooks/tradesafe/:secret` was dispatching only to `TradeSafeTransactionCallbackHandler.handle()` (audit-only — writes one AuditLog row) and never to `TradeSafeWebhookHandler.handleFundsReceived()` (the ledger-posting handler). The previous "Single-rail TradeSafe architecture" entry's claim about the FUNDS_RECEIVED webhook was the design intent, not the as-shipped state. Mock mode masked the bug end-to-end. Fix injects `TradeSafeWebhookHandler` as a 5th constructor arg on the route controller; after the audit handler runs, it branches on the AUTHORITATIVE re-fetched `transaction.state` — `FUNDS_RECEIVED` → `handleFundsReceived(payload)`. Other states (CREATED, INITIATED, FUNDS_RELEASED, CANCELLED, REFUNDED, DISPUTE) remain forensic-only until Phase 4 lands. **Wiring contract** is locked behind 6 controller-spec tests covering the dispatch by-state matrix + handler-failure + null-re-fetch defence (we never trust the body's claimed state). **Payments service gap coverage** added 14 spec tests for guards the prior 7-test set didn't pin (soft-deleted bounty, cross-brand RBAC + SUPER_ADMIN bypass, already-PAID, zero face value, multi-reward integer-cents sum, BUYER+SELLER+AGENT party assertion, feeAllocation=BUYER, industry env, totalValueCents persistence, resolveFundingStatus paths). **Cooldown corrections**: (a) the `?? agentToken` fallback in `tradesafe-payments.service.ts:177-185` was dead — `config.get('X', '')` returns `''` and `'' ?? Y` is `''`, so an unset `TRADESAFE_DEFAULT_BUYER_TOKEN` would silently send empty BUYER token in live mode. Switched to `||`; (b) token-cache stampede guard in `tradesafe-graphql.client.ts:getToken` now tracks lock ownership locally — only releases the lock if WE acquired it (prior code's `redis.del` in `finally` could blow away a peer's still-active lock), and re-attempts `setNxEx` after the 1s polling exhausted before falling through (peer-crashed-mid-fetch case). 2 regression tests for the agent-token fallback (`buyer-token unset → agent-token used`; same for seller). **Dead-code deletion landed** (commit `6ef1595`, 2026-04-27): the legacy `TradeSafeClient` REST shim (~240 LoC), `TradeSafePayoutAdapter`, `PayoutProviderFactory`, `payout-provider.interface.ts`, and three accompanying spec files were removed (-816 LoC, -1 suite, -22 tests). All seven files were unreachable post-Phase-3 — `PayoutsService.initiatePayout` throws `NotImplementedException` before any caller could reach them. When Phase 4 outbound payouts land (auto-release on submission approval), the new code goes straight through `TradeSafeGraphQLClient` (using `allocationStartDelivery` / `allocationAcceptDelivery` mutations) — no factory + adapter indirection needed for a single rail. `docs/deployment/tradesafe-live-readiness.md` carries a top-of-doc banner marking it as pre-Phase-3 historical reference (its env-var matrix + token-cache lessons remain useful, the wiring details do not). Test state: 72 suites / 1295 passed / 3 skipped (was 73 / 1260 / 3 pre-pass). Routing semantics for future contributors: the URL-secret-callback path runs BOTH the audit handler (always) AND a state-specific dispatcher (currently only `FUNDS_RECEIVED` maps to a ledger handler).
- **KYB hardening — capture flow + admin tooling + TradeSafe `tokenCreate` sync** (2026-04-27, branch `payment-gateway-prelaunch`, 14 commits, ~3900 LoC, ~30 files) — R24 pre-launch foundation that turns the existing `kybStatus` enum from a value with nothing behind it into a real verification flow. Schema migration `20260427000000_kyb_hardening` adds 11 persistent KYB columns to the Brand model (`kybRegisteredName`, `kybTradeName`, `kybRegistrationNumber`, `kybVatNumber`, `kybTaxNumber`, `kybCountry`, `kybContactEmail`, `kybOrgType`, `kybRejectionReason`, `kybRejectedAt`, `tradeSafeTokenId @unique`), a new `KybDocument` child table (separate primitive from the submission-scoped `FileUpload` — KYB docs outlive any submission), two new enums (`KybOrgType`, `KybDocumentType`), and a composite `(kybStatus, kybSubmittedAt)` index for the admin queue. Migration is fully idempotent (CREATE … IF NOT EXISTS + DO $$ EXCEPTION pattern from batch 13A). **Backend (Wave 1)**: `KybService.submit` persists all form fields to the Brand row (was AuditLog-only before) and tightens the state guard so already-PENDING can't re-submit; resubmit-from-REJECTED clears the stale rejection. `KybService.approve` mints a TradeSafe BUYER party token via `TradeSafeGraphQLClient.tokenCreate` (with the brand's full org details — registeredName, tradeName, orgType, registrationNumber, taxNumber) and stores it on `Brand.tradeSafeTokenId`; mock mode synthesises `mock-brand-token-{brandId}`; on tokenCreate failure the brand stays APPROVED + the failure is audit-logged (`BRAND_TRADESAFE_TOKEN_CREATE_FAILED`) so operators can intervene. `TradeSafePaymentsService.createBountyFunding` now prefers `bounty.brand.tradeSafeTokenId` over `TRADESAFE_DEFAULT_BUYER_TOKEN` env, falling back through agent-token chain. New `KybDocumentsService` handles upload/list/delete with RBAC (BUSINESS_ADMIN of brand or SUPER_ADMIN), state-guards (no uploads after APPROVED, no deletes when uploader-id mismatch + non-SUPER_ADMIN), 10MB size cap, mime allow-list (pdf/jpeg/png), notes ≤500 chars, **expiresAt past-date guard**, and a path-traversal-guarded download endpoint at `GET /brands/:brandId/kyb/documents/:documentId/download` that streams the file via `fs.createReadStream` (matches `BrandAsset` / `FileUpload` storage convention — disk path on the row, HTTP URL computed at serialization time). New admin endpoints `GET /admin/brands/kyb` (paginated PENDING queue) + `GET /admin/brands/:brandId/kyb/review` (full submission view including documents + last-50 KYB-related audit log entries). 6 new audit actions (`KYB_SUBMIT`, `KYB_APPROVE`, `KYB_REJECT`, `KYB_DOCUMENT_UPLOADED`, `KYB_DOCUMENT_DELETED`, `BRAND_TRADESAFE_TOKEN_CREATED`, `BRAND_TRADESAFE_TOKEN_CREATE_FAILED`). **Frontend (Wave 2)**: `apps/web/src/app/business/brands/kyb/page.tsx` rewritten with four status panels (NOT_STARTED form / PENDING summary / REJECTED reason + resubmit / APPROVED locked) and a real document upload section (type picker / file input / expiry / notes / list with view+delete). New admin queue at `apps/web/src/app/admin/brands/kyb/page.tsx` (oldest-first DataTable, days-waiting color coding) plus `KybSection` + `KybDocumentsCard` + `KybAuditTimeline` + `RejectKybDialog` components on the brand detail page. `StatusBadge` gained a `kyb` variant (amber pulse PENDING / emerald APPROVED / rose REJECTED / slate NOT_STARTED). `useBrand` extended with `useSubmitKyb` (new payload), `useKybDocuments`, `useUploadKybDocument`, `useDeleteKybDocument`. `useAdmin` extended with `useAdminPendingKyb`, `useAdminKybReview`, `useApproveKyb`, `useRejectKyb`. New "KYB Review" sidebar nav entry. **Test surface**: 74 suites / 1340 passed / 3 skipped on the api side (+45 from baseline 1295). Web jest 10 suites / 307 passed unchanged; web typecheck clean; `next build` constraint **not** verified due to a pre-existing `shadow-level-2` Tailwind config gap unrelated to KYB. **Out of scope deferred to follow-up**: admin retry endpoint for tradesafe tokenCreate failures, expiry-revoke scheduler, live-count badge on the KYB Review nav, email notifications on status change, RFC-5987 filename encoding in `Content-Disposition` (pre-existing pattern across `FilesController` + `kyb.controller.ts` — separate hygiene PR). **Operator follow-ups for go-live**: once R24 prod creds land + `TRADESAFE_MOCK=false`, the `tokenCreate` call on KYB approve becomes live — test against TradeSafe sandbox first; admin manual chase replaces email notifications until operator-flag warrants the build.

**Documentation layout (reorganized 2026-04-15, committed in `b4f96ac`):**
- Repo root now holds only `claude.md`; four loose docs (`AGENTS.md`, `RELEASE-NOTES.md`, `SPRINT-PLAN.md`, `payment-gateway-review.docx`) moved into `md-files/` on 2026-04-15. The fifth (`DESIGN-SYSTEM.md`) was removed on 2026-04-18 when the Claude Design handoff bundle replaced it — canonical location is now `apps/web/src/styles/design-system/`.
- Nine per-role agent specs moved into `md-files/agents/` (entry point: `md-files/agents/agent-overview.md`).
- Outdated 2026-03-27 codebase audit archived at `md-files/archive/AUDIT-REPORT-2026-03-27.md`; still cited from `docs/SECURITY-COMPLIANCE.md` and `docs/BACKUP-STRATEGY.md` as historical evidence.
- Live cross-references rewritten in 13 files (2 ADRs, `docs/contributing.md`, `docs/BACKUP-STRATEGY.md`, `docs/SECURITY-COMPLIANCE.md`, 2 review audits, etc.). `git mv` preserved history on all tracked moves.

**Explicitly gated — do not remove without Team Lead sign-off:**
- `TRADESAFE_MOCK=true` — mock mode on Render until prod OAuth creds arrive (R24). Flipping requires live TradeSafe credentials per ADR 0011.

**Out of scope for this MVP cycle:**
- **Pro subscriptions** — TradeSafe has no recurring-subscription primitive; UI retains a "Pro subscriptions coming soon" placeholder (ADR 0011 §7). Enabling Pro becomes a separate workstream with a subscription-capable PSP.
- **Multi-brand spending wallets** — per-bounty escrow via TradeSafe does not support pre-funded brand wallets. Future enterprise-tier feature if commercial demand justifies it.

**Open risks:** R24 (TradeSafe prod OAuth creds — external blocker, the sole gate between `TRADESAFE_MOCK=true` and real-money flow). R25/R26/R27/R28/R29/R30/R32/R33/R34/R35 closed. Post-cutover residuals:
- **R36** (Open) — no auto-retry after kill-switch-blocked inbound webhook; TradeSafe callback handler returns 2xx, no retry. Recovery via reconciliation + compensating entry.
- **R37** (Open) — TradeSafe fee-allocation pattern for the three platform fee streams (20% hunter commission / 15% brand admin fee / 5% transaction fee) vs TradeSafe's single `PartyInput.fee` tuple on AGENT party. See ADR 0011 §8 OQ-1 — resolved via first live sandbox smoke.
- **B1** (Closed 2026-04-18, commit `5ba7b60`) — `STITCH_API_BASE` URL fix in go-live-checklist (now historical).
- **B2+B3** (**Closed 2026-04-18**, commit `efd3760`) — `docs/INCIDENT-RESPONSE.md` gains §5.5 kill-switch procedure + TradeSafe payment-provider contact placeholders. Pre-launch banner added at top warning contact placeholders must be filled before cutover.

**Engineering state for single-rail TradeSafe flip is complete:** historical schema migration live on staging (drop Stitch tables), env validation in place, OAuth client-credentials flow working against sandbox, webhook handlers exist, reconciliation covers the TradeSafe branch. R24 prod creds is the final gate.

**Production deployment state (2026-04-25):** Both API and web services are live on Render Frankfurt (`api.socialbounty.cash` + `www.socialbounty.cash` / apex). DNS resolved, Cloudflare in front, TLS via Render's managed certs. Auth flow (OTP via Brevo SMTP) verified end-to-end. Render auto-deploys both services on push to main. CI gates the deploy via `.github/workflows/ci.yml`. `TRADESAFE_MOCK=true` keeps real money out of the rail until R24 lands.

**References for future agents:**
- `docs/adr/0001` through `docs/adr/0012` — 12 architectural decisions. ADR 0011 is the canonical payment spec post-2026-04-24; ADR 0012 is the canonical multi-channel OTP (Email + SMS) spec post-2026-04-25.
- `docs/reviews/2026-04-15-team-lead-audit-batch-*.md` — audits for batches 2–11, 13 (batches 12 & 14 delivered without formal audit write-ups; individual agents self-verified).
- `docs/reviews/2026-04-15-r28-migration-reconciliation.md` — migration reconciliation evidence.
- `docs/reviews/2026-04-15-orphan-sweep.md` — codebase hygiene inventory.
- `docs/perf/2026-04-15-reconciliation-benchmarks.md` — perf report + mitigation evidence.
- `apps/web/src/styles/design-system/` — canonical design system (Claude Design handoff bundle, 2026-04-18). README + SKILL + token CSS + component CSS + 5 preview HTMLs + wordmark asset.

---

# Knowledge Base & Financial Integrity Framework

## 1. Purpose

This section extends the rules above with an operational framework for:
- Financial integrity protection
- Recurring-issue detection
- Claude decision support before any fix

This is not a changelog. It is a system-memory and control layer.

## 2. Core Principle

If an issue recurs, treat it as a structural flaw, not a bug. Every fix must aim to eliminate recurrence, not just the symptom.

## 3. Agent Routing (extends Hard Rule #8)

All development work routes through `md-files/agents/agent-overview.md` → `md-files/agents/agent-team-lead.md`.

Claude may:
- Analyse the task
- Propose a root cause and candidate fix
- Draft code or schema changes for review

Claude must not:
- Merge, deploy, or finalise implementation without Agent Team Lead assignment
- Bypass the agent workflow for "quick fixes" — especially financial ones

## 4. Financial Non-Negotiables

Payment provider: **TradeSafe** (inbound + outbound unified rail per ADR 0011). See `docs/adr/0011-tradesafe-unified-rail.md` for the canonical payment spec — fee rates, party roles (BUYER/SELLER/AGENT), state machines, webhook strategy (URL-path secret + GraphQL re-fetch), and idempotency rules all live there.

Every financial mutation must satisfy all of the following:

1. **Double-entry**: debits equal credits within the transaction.
2. **Idempotency**: enforced at the DB level via `UNIQUE(referenceId, actionType)`.
3. **Transaction group integrity**: all steps commit or roll back together (single DB transaction or saga with compensation).
4. **Integer minor units**: amounts stored as integers (cents), never floats.
5. **Append-only ledger**: no updates or deletes; corrections are compensating entries.
6. **AuditLog entry**: required for every mutation (ties to Hard Rule #3).
7. **Retry-safe**: handlers must tolerate timeouts, retries, and webhook replays.
8. **Platform custody**: all funds flow through platform-controlled custody. No direct brand-to-hunter payments.
9. **Plan snapshot**: tier (Free / Pro) is snapshotted onto each transaction at creation time. In-flight transactions are never re-priced on plan change.
10. **Global fee independence**: the 3.5% global platform fee is calculated independently of tier admin fee / commission, stored in its own ledger account (`global_fee_revenue`), and shown as a separate line in all UI and reports.

Violating any of these without written justification in the PR is a blocker.

## 5. Required Tests for Financial Code

Every function that writes to the ledger must have:
- Unit test: happy path
- Unit test: duplicate/retry call produces no second entry
- Integration test: partial failure rolls back the full transaction group
- Integration test: webhook replay is idempotent

No ledger-touching PR merges without these.

## 6. Known Failure Patterns (check first)

Before proposing a fix, Claude checks `md-files/knowledge-base.md` for prior occurrences of:
- Idempotency failures
- Race conditions on shared resources (wallets, claims)
- Retry duplication
- Webhook replay
- Missing input validation
- Config drift between environments

## 7. Claude's Workflow for Any Fix

1. Read `md-files/knowledge-base.md` for similar prior incidents.
2. If payment-related, read `docs/adr/0011-tradesafe-unified-rail.md` (canonical) and `md-files/financial-architecture.md` (mechanics) and verify the change against Section 4.
3. Identify root cause — not just the symptom. State it explicitly.
4. If the issue has occurred before, propose a structural fix, not a patch.
5. Route implementation via Agent Team Lead.
6. After resolution, log a KB entry (template: `md-files/knowledge-base.md#entry-template`).

## 8. Definitions

**Recurrence**: two or more occurrences of the same root cause within 90 days. A recurring issue requires a structural fix, not a patch.

**Severity**:
- **Critical**: financial loss possible, data corruption, or auth bypass.
- **High**: user-facing failure of a core flow (claim, submit, payout).
- **Medium**: degraded experience with a workaround.
- **Low**: cosmetic or rare edge case.

**Financial Impact**:
- **Critical**: incorrect money movement possible.
- **High**: reconciliation drift without user loss.
- **Medium**: reporting or display error only.
- **Low / None**: no ledger or balance effect.

## 9. Automatic KB Entry Triggers

A KB entry is mandatory whenever any of these occur:
- Duplicate transaction detected
- Ledger imbalance detected
- Reconciliation mismatch
- Same root cause seen twice (recurrence threshold hit)
- Any Critical or High financial-impact incident

## 10. Escalation

On detection of a Critical severity or Critical financial-impact issue, Claude must:
1. Stop proposing code changes.
2. Recommend invoking the Financial Kill Switch (pause payouts) if ledger integrity is in doubt.
3. Flag the issue to Agent Team Lead with severity, suspected root cause, and affected transaction groups.

## 11. Referenced Files

- `docs/adr/0011-tradesafe-unified-rail.md` — **canonical** TradeSafe single-rail spec: flows, fees, clearance, states, webhooks, refunds, reconciliation rules
- `md-files/financial-architecture.md` — ledger mechanics, idempotency patterns, reconciliation engine
- `md-files/knowledge-base.md` — KB schema, entry template, example entries
- `md-files/admin-dashboard.md` — Finance Reconciliation Dashboard, kill switch, KB insights
- `md-files/implementation-phases.md` — Phase 1–4 delivery plan (payments → reconciliation → dashboard → KB automation)
- TradeSafe GraphQL API: https://docs.tradesafe.co.za/ (sandbox: `https://api-developer.tradesafe.dev/graphql`; production: `https://api.tradesafe.co.za/graphql`)
- TradeSafe OAuth: `https://auth.tradesafe.co.za/oauth/token` (client-credentials grant)

## graphify

This project has a graphify knowledge graph at `graphify-out/`. Current state (rebuilt 2026-04-24): **2498 nodes · 4728 edges · 64 communities · 543 files indexed · 58% EXTRACTED / 42% INFERRED edges (avg confidence 0.85)**. Top god nodes: `Social Bounty MVP` (28 edges), `FinanceAdminService` (26 edges).

### Rules

- Before answering architecture or codebase questions, read `graphify-out/GRAPH_REPORT.md` for god nodes and community structure.
- `graphify-out/wiki/nodes/` holds 50 LLM-written 1–2-paragraph articles for the top-50 nodes by degree (filename slug = node id, stable across re-clusterings). Start there for any named class, service, controller, or ADR before reading raw files. `graphify-out/wiki/index.md` is sorted by degree.
- For cross-module "how does X relate to Y" questions, prefer `graphify query "<question>"`, `graphify path "<A>" "<B>"`, or `graphify explain "<concept>"` over grep — these traverse the graph's EXTRACTED + INFERRED edges instead of scanning files.
- The graph auto-rebuilds on every `git commit` via a post-commit husky hook (AST-only, no LLM cost). No manual `graphify update .` needed for code changes. If `.husky/post-commit` is missing, re-run `bash scripts/install-graphify-hook.sh` to reinstall.
- For docs/image changes (which require semantic re-extraction), run `graphify update .` manually.

### Using the MCP server

`.mcp.json` registers a `graphify` stdio MCP server at project scope. Parallel sub-agents can call `query_graph`, `shortest_path`, `get_community`, `god_nodes`, `graph_stats` without loading the full `graph.json` into their context. First-time setup requires the `mcp` Python package in graphify's env: `uv tool install --reinstall graphifyy --with mcp --with sentence-transformers`.

### What the graph contains (and what it used to)

The graph is **AST-extraction only** as of 2026-04-24. Previous builds layered on hand-curated enrichments that lived in `graph.json` as data, not as reproducible scripts — so the auto-rebuild wipes them on every commit. Between 2026-04-17 and 2026-04-24 the post-commit hook wasn't firing (`.husky/post-commit` was missing), so the enrichments persisted in the committed `graph.json` and CLAUDE.md advertised them. When the hook was reinstalled on 2026-04-24 and 15 commits of drift were reconciled, the enrichments were wiped.

Enrichments that no longer exist in the graph (all were documented here previously): flow hyperedges (`bounty_funding_flow`, `hunter_submission_lifecycle`, `kill_switch_override_bypass`, `subscription_upgrade_flow`, `brand_onboarding_flow`, `auto_refund_visibility_flow`, `tradesafe_outbound_rail`), doc→code rationale edges (20), test↔impl edges (57), config→target edges (20), e2e→page edges (21), semantic similarity edges (1146 via `all-MiniLM-L6-v2`), git-activity overlay (`last_touched_days_ago`, `commits_last_90d`, `commits_total`), test-type refinement (unit/integration/e2e split), and a dedupe pass that merged 570 redundant nodes. Original implementations are in commits `d51e774`, `8377896`, `89ca675`, `7935aaa` — reconstructing them as a post-rebuild step (so they survive hook fires) is a dedicated workstream.

Residual artifacts still on disk but no longer linked into the graph:
- `graphify-out/wiki/nodes/` — 50 top-node articles from 2026-04-23 (node IDs stable, so still valid).
- `graphify-out/AUDIT.md` — completeness audit from 2026-04-23 (describes pre-rebuild state; 100% file coverage, 91.5% main component).
- `graphify-out/.community-labels.json` — content-based community labels from 2026-04-23 (community IDs have since shifted; labels may not map cleanly onto the 64 current communities).
- `graphify-out/.embeddings.npz` — MiniLM sentence-embedding cache (still usable if the similarity-edge script is re-run).

### Tooling exclusions

- `.graphifyignore` excludes `node_modules/`, `dist/`, `.next/`, build artefacts, secrets, and third-party clones.
- `scripts/check-kill-switch-bypass.sh` excludes `graphify-out/**` (the graph quotes ADR 0006 prose as descriptive data; same class of exclusion as `docs/reviews/` and the ADR itself).
- `.gitignore` excludes `graphify-out/cache/`, `graphify-out/manifest.json`, `graphify-out/cost.json`, `graphify-out/.embeddings*` — everything else under `graphify-out/` is committed.

### Onboarding automation

`npm install` triggers `scripts/install-graphify-hook.sh` (via the `prepare` script) which idempotently appends the graphify block to husky's `post-commit` + `post-checkout` hooks. Silently no-ops if `graphify` isn't on PATH, so `npm install` never fails — just prints a one-line install hint (`uv tool install graphifyy --with mcp`). The Docker build deletes the root `prepare` script via `npm pkg delete scripts.prepare` before `npm ci` (see `apps/api/Dockerfile:57`) — this is intentional (no `.git` in the image, scripts would fail) and does not affect the local repo's hook state.
